<div class="menu">
    <h2 class="extra-bold"> <i class="fa fa-lg icon-section magenta"></i> Section Quick Links</h2>
    <ul class="list-unstyled">
        {% for item in page.sections %}
        <li>
        {% if item.link and item.text %}
        <a href="{{site.baseurl}}{{page.location}}#{{item.link}}">{{item.text}}</a>
        {% else %}
        <a href="{{site.baseurl}}{{page.location}}#{{item}}">{{item}}</a>
        {% endif %}
        <br/>
        <p class="small"> {{item.description}} </p>
        </li>
        {% endfor %}
    </ul>
</div>