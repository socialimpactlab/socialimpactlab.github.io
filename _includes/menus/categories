<div class="menu">
    <h2><span class="lighter">Filter</span> Categories </h2>
    <ul class="list-unstyled">
        {% for item in site.collections %}
        {% unless item.title == "Resources" %}
        <li>
            <div><p><a class="extra-bold" href="{{site.baseurl}}{{item.root}}">{{item.title}}</a><br/><span>{{item.description}}</span></p></div>
        </li>
        {% endunless %}
        {% endfor %}
    </ul>
    <p class="foot">Read <span class="extra-bold">Stories</span> <a href="{{site.baseurl}}/blog"  class="fa fa-chevron-circle-right "></a></p>
</div>